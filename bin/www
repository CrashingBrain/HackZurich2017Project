#!/usr/bin/env node
var debug = require('debug')('nepeta');
var app = require('../index');

app.set('port', process.env.PORT || 3001);

var server = app.listen(app.get('port'), function() {
  debug('NEPETA server listening on port ' + server.address().port);
});
